#


#include<stdio.h>


char *readFromFile(char*);

	FILE *file= fopen(filename:fileName, modes; "r");
	char *textContent = (char*) malloc (size: sizeof (char )* MAX_SIZE);
	
	int ch fgetc(stream:flie);
	int count =0;
	
	while (ch!= EOF){
			textContent[count++]= (char)ch;
			ch=fgetc(stream:)
	} 

#endif
